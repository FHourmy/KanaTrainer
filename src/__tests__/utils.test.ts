describe("getRandomKana function", () => {
	test.todo("returns a single kaba of the desired type");
});

describe("compareCanvas", () => {
	test.todo("2 equals canvas returns 0");
	test.todo("2 different canvas returns > 0");
	test.todo("if can't get context of canvas returns 0");
});
